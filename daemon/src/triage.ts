// triage.ts — entry: claim + route + dispatch ONE bead now (§2.1, §3.2 triage verb).
// v1 scaffold stub; shares tick internals in a later bead.

export function main(): void {
  console.log("triage: stub");
}

main();
