// status.ts — entry: render state.json snapshot as the aerial pipeline view (§3.2).
// v1 scaffold stub; the renderer lands in a later bead.

export function main(): void {
  console.log("status: stub");
}

main();
