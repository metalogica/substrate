// serve.ts — entry: preflight → boot-reap → tick loop → SIGINT handler (§2.1).
// v1 scaffold stub; the tick loop lands in later beads.

export function main(): void {
  console.log("serve: stub");
}

main();
