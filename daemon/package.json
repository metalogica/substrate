{
  "name": "@substrate/serve-daemon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Substrate serve-v1 daemon: local-first pull daemon that runs a graphed bead queue as headless sessions.",
  "scripts": {
    "gate": "tsc --noEmit && vitest run",
    "serve": "tsx src/serve.ts",
    "status": "tsx src/status.ts",
    "tidy": "tsx src/tidy.ts",
    "triage": "tsx src/triage.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "yaml": "^2.6.1"
  }
}
