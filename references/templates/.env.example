# Client-safe (exposed to the browser)
VITE_CONVEX_URL=
VITE_CLERK_PUBLISHABLE_KEY=

# Server-only (NEVER commit, NEVER expose to client)
CLERK_SECRET_KEY=
CLERK_JWT_ISSUER_DOMAIN=
# Webhook signing secret from Clerk Dashboard → Webhooks → Endpoint.
# Starts with `whsec_`. Do NOT paste the webhook URL here.
CLERK_WEBHOOK_SECRET=
