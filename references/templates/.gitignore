node_modules
dist
# All env files (.env, .env.local, .env.prod, .env.production.local, etc.)
# .env.example IS tracked — it's the template without real values.
.env*
!.env.example
.convex
.vercel
.DS_Store
*.log
coverage

# Generated by tooling — regenerates on demand
convex/_generated/
src/routeTree.gen.ts

# Convex CLI writes these per-project
.agents/
skills-lock.json

# Local-only reference (kept after /substrate-migrate)
prototype-archive/
